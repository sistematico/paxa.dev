---
interface Props {
	date: Date;
}

const { date } = Astro.props;
---

<time class="text-white" datetime={date.toISOString()}>
	{
		date.toLocaleDateString('pt-br', {
			year: 'numeric',
			month: 'short',
			day: 'numeric',
		})
	}
</time>
