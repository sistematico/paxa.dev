"use client";
import NextTopLoaderComponent from "nextjs-toploader";
import type { NextTopLoaderProps } from "nextjs-toploader";

export default function TopLoader(props: NextTopLoaderProps) {
  return <NextTopLoaderComponent {...props} />;
}
