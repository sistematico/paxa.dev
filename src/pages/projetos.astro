---
// import aboutImg from "@/assets/about-astro.png";
// @ts-ignore
import { Icon } from "astro-icon/components";
// @ts-ignore
import PageLayout from "@/layouts/Base.astro";
// import { Image } from "astro:assets";

const meta = {
	description: "Sobre o profissional",
	title: "Sobre",
};
		

const features = [
	{
		title: "FIPE",
		description: "Exemplo de uso a API da tabela FIPE usando o Vue.js",
		icon: "bx:bxs-briefcase",
		url: "https://sistematico.github.io/fipe",
		repo: "https://sistematico.github.io/feriados"
	},
	{
		title: "Feriados",
		description: "API de Feriados",
		icon: "bx:bxs-window-alt",
		url: "https://sistematico.github.io/feriados",
		repo: "https://sistematico.github.io/feriados"
	},
];
---

<PageLayout meta={meta}>
	<div class="space-y-6">
		<h1 class="title mb-6">Projetos</h1>
		<p class="mb-4">Alguns projetos que participo.</p>
		<div class="grid sm:grid-cols-1 md:grid-cols-2 mt-16 gap-16">
			{
				features.map((item) => (
					// <div class="container mx-auto p-6">
					// 	<div class="flex items-stretch -mx-4">
					// 		<div class="flex-1 p-4">
					// 			<div class="block bg-white overflow-hidden border-2 h-full">
					// 				<div class="p-4">
					// 					<h2 class="mt-2 mb-2 font-bold text-2xl font-Headingg">
					// 						Card Name
					// 					</h2>
					// 					<div class="mb-4 flex flex-wrap">
					// 						<span class="mr-2">Link 1</span>
					// 						<span>Link 2</span>
					// 					</div>

					// 					<p class="text-md text-justify">Some Description</p>
					// 				</div>
					// 				<div class="p-4 flex flex-wrap items-center">
					// 					<p class="px-1 py-2 tracking-wide text-xs mr-2 mb-2">Tag #1</p>
					// 					<p class="px-1 py-2 tracking-wide text-xs mr-2 mb-2">Tag #2</p>
					// 				</div>
					// 			</div>
					// 		</div>
							
					// 		<div class="flex-1 p-4">
					// 			<div class="block bg-white overflow-hidden border-2 h-full">
					// 				<div class="p-4">
					// 					<h2 class="mt-2 mb-2 font-bold text-2xl font-Headingg">
					// 						Card Name
					// 					</h2>
					// 					<div class="mb-4 flex flex-wrap">
					// 						<span class="mr-2">Link 1</span>
					// 						<span>Link 2</span>
					// 					</div>

					// 					<p class="text-md text-justify">Some Description Lorem ipsum dolor sit amet, consectetur adipiscing elit. Maecenas vel enim lectus.</p>
					// 				</div>
					// 				<div class="p-4 flex flex-wrap items-center">
					// 					<p class="px-1 py-2 tracking-wide text-xs mr-2 mb-2">Tag #1</p>
					// 					<p class="px-1 py-2 tracking-wide text-xs mr-2 mb-2">Tag #2</p>
					// 				</div>
					// 			</div>
					// 		</div>
					// 	</div>
					// </div>


					<div class="relative">
						<div class="flex gap-4 items-start">
							<div class="mt-1 bg-black rounded p-2 w-8 h-8 shrink-0">
								<Icon class="text-white" name={item.icon} />
							</div>
							<div>
								<h3 class="font-semibold text-lg">{item.title}</h3>{" "}
								<p class="text-slate-500 mt-2 leading-relaxed">{item.description}</p>
								<p>
									<a class="after:absolute after:inset-0" href={item.url}>demo</a>
									<a class="after:absolute after:inset-0" href={item.repo}>repo</a>
								</p>
							</div>
						</div>
					</div>
				))
			}
		</div>
	</div>
</PageLayout>
