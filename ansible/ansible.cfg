[defaults]
inventory = ./inventory.ini
host_key_checking = False
retry_files_enabled = False

[ssh_connection]
# ControlPersist speeds up repeated connections
control_path = ~/.ansible/cp/ansible-%%h-%%p-%%r
